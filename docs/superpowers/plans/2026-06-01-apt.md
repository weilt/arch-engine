# Agent-Protocol-Toolkit (APT) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 构建可全局安装的 APT 工具集（MCP Server + agent-init + 命令模板 + install 脚本），跨 macOS/Windows，强制多代理契约查询与注册流程。

**Architecture:** 源码在本仓库；`install.sh/ps1` 构建 MCP 并部署到 `~/.apt/`、合并全局 Claude MCP 配置；`agent-init` 向项目注入 slash 命令并初始化 `.ai/db.json`；MCP Server 通过 stdio 暴露三个 Tool，读写 cwd 下 `.ai/` 数据。

**Tech Stack:** Node.js 18+, TypeScript, `@modelcontextprotocol/sdk`, Shell, PowerShell

**Spec:** `docs/superpowers/specs/2026-06-01-apt-design.md`

---

## File Map

| 文件 | 职责 |
|------|------|
| `mcp-server/src/paths.ts` | 解析 `.ai/db.json`、TS 文件绝对路径 |
| `mcp-server/src/db.ts` | 读写 db.json，contracts/missing CRUD |
| `mcp-server/src/index-md.ts` | 从 db 全量生成 INDEX.md |
| `mcp-server/src/index.ts` | MCP stdio 入口，注册 3 Tools |
| `mcp-server/tests/db.test.ts` | db 模块单元测试 |
| `mcp-server/tests/index-md.test.ts` | INDEX 生成测试 |
| `templates/start-feature.md` | /start-feature 命令 |
| `templates/finish-feature.md` | /finish-feature 命令 |
| `bin/agent-init.sh` | Unix 项目初始化 |
| `bin/agent-init.ps1` | Windows 项目初始化 |
| `bin/agent-init.cmd` | Windows PATH 友好入口 |
| `scripts/install.sh` | Unix 全局安装 |
| `scripts/install.ps1` | Windows 全局安装 |
| `README.md` | 使用说明 |

---

### Task 1: MCP Server 项目脚手架

**Files:**
- Create: `mcp-server/package.json`
- Create: `mcp-server/tsconfig.json`
- Create: `mcp-server/vitest.config.ts`

- [ ] **Step 1: 创建 package.json**

```json
{
  "name": "agent-protocol-mcp",
  "version": "1.0.0",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "agent-protocol-mcp": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "dev": "tsc --watch"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.8.0",
    "vitest": "^3.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
```

- [ ] **Step 2: 创建 tsconfig.json**

```json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "Node16",
    "moduleResolution": "Node16",
    "outDir": "dist",
    "rootDir": "src",
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "declaration": true
  },
  "include": ["src/**/*"],
  "exclude": ["node_modules", "dist", "tests"]
}
```

- [ ] **Step 3: 创建 vitest.config.ts**

```typescript
import { defineConfig } from "vitest/config";

export default defineConfig({
  test: {
    environment: "node",
  },
});
```

- [ ] **Step 4: 安装依赖并验证**

Run: `cd mcp-server && npm install && npm run build`  
Expected: 空 src 时 build 可能失败 — 下一步添加 src 后再 build

---

### Task 2: paths 模块

**Files:**
- Create: `mcp-server/src/paths.ts`
- Create: `mcp-server/tests/paths.test.ts`

- [ ] **Step 1: 写 failing test**

```typescript
// mcp-server/tests/paths.test.ts
import { describe, it, expect } from "vitest";
import path from "node:path";
import { getAiDir, getDbPath, resolveTsPath } from "../src/paths.js";

describe("paths", () => {
  const root = "/project";

  it("getAiDir returns .ai under cwd", () => {
    expect(getAiDir(root)).toBe(path.join(root, ".ai"));
  });

  it("getDbPath returns db.json path", () => {
    expect(getDbPath(root)).toBe(path.join(root, ".ai", "db.json"));
  });

  it("resolveTsPath handles relative path", () => {
    expect(resolveTsPath(root, "src/contracts/foo.ts")).toBe(
      path.join(root, "src/contracts/foo.ts")
    );
  });

  it("resolveTsPath handles absolute path", () => {
    const abs = path.join(root, "abs.ts");
    expect(resolveTsPath(root, abs)).toBe(abs);
  });
});
```

- [ ] **Step 2: 运行 test 确认 FAIL**

Run: `cd mcp-server && npm test`  
Expected: FAIL — module not found

- [ ] **Step 3: 实现 paths.ts**

```typescript
// mcp-server/src/paths.ts
import path from "node:path";

export function getProjectRoot(cwd: string = process.cwd()): string {
  return cwd;
}

export function getAiDir(projectRoot: string): string {
  return path.join(projectRoot, ".ai");
}

export function getDbPath(projectRoot: string): string {
  return path.join(getAiDir(projectRoot), "db.json");
}

export function getIndexMdPath(projectRoot: string): string {
  return path.join(getAiDir(projectRoot), "INDEX.md");
}

export function resolveTsPath(projectRoot: string, tsFilePath: string): string {
  return path.isAbsolute(tsFilePath)
    ? tsFilePath
    : path.join(projectRoot, tsFilePath);
}
```

- [ ] **Step 4: 运行 test 确认 PASS**

Run: `cd mcp-server && npm test`  
Expected: paths tests PASS

---

### Task 3: db 模块

**Files:**
- Create: `mcp-server/src/db.ts`
- Create: `mcp-server/tests/db.test.ts`

- [ ] **Step 1: 定义类型与 emptyDb**

```typescript
// mcp-server/src/db.ts
import fs from "node:fs/promises";
import path from "node:path";
import { getAiDir, getDbPath } from "./paths.js";

export interface Contract {
  name: string;
  description: string;
  tsFilePath: string;
  registeredAt: string;
}

export interface MissingRequest {
  missingName: string;
  reason: string;
  reportedAt: string;
}

export interface AptDb {
  contracts: Contract[];
  missingRequests: MissingRequest[];
}

export function emptyDb(): AptDb {
  return { contracts: [], missingRequests: [] };
}
```

- [ ] **Step 2: 写 db.test.ts（read/write/find）**

```typescript
// mcp-server/tests/db.test.ts
import { describe, it, expect, beforeEach, afterEach } from "vitest";
import fs from "node:fs/promises";
import path from "node:path";
import os from "node:os";
import {
  readDb,
  writeDb,
  findContract,
  appendContract,
  appendMissing,
} from "../src/db.js";

describe("db", () => {
  let tmpDir: string;

  beforeEach(async () => {
    tmpDir = await fs.mkdtemp(path.join(os.tmpdir(), "apt-test-"));
    await fs.mkdir(path.join(tmpDir, ".ai"), { recursive: true });
  });

  afterEach(async () => {
    await fs.rm(tmpDir, { recursive: true, force: true });
  });

  it("readDb throws if db missing", async () => {
    await expect(readDb(tmpDir)).rejects.toThrow(/not found/i);
  });

  it("writeDb and readDb roundtrip", async () => {
    const db = { contracts: [], missingRequests: [] };
    await writeDb(tmpDir, db);
    const loaded = await readDb(tmpDir);
    expect(loaded).toEqual(db);
  });

  it("appendContract rejects duplicate name", async () => {
    await writeDb(tmpDir, { contracts: [], missingRequests: [] });
    await appendContract(tmpDir, {
      name: "Foo",
      description: "d",
      tsFilePath: "a.ts",
      registeredAt: new Date().toISOString(),
    });
    await expect(
      appendContract(tmpDir, {
        name: "Foo",
        description: "d2",
        tsFilePath: "b.ts",
        registeredAt: new Date().toISOString(),
      })
    ).rejects.toThrow(/already exists/i);
  });

  it("appendMissing adds record", async () => {
    await writeDb(tmpDir, { contracts: [], missingRequests: [] });
    await appendMissing(tmpDir, {
      missingName: "Bar",
      reason: "needed",
      reportedAt: new Date().toISOString(),
    });
    const db = await readDb(tmpDir);
    expect(db.missingRequests).toHaveLength(1);
    expect(db.missingRequests[0].missingName).toBe("Bar");
  });
});
```

- [ ] **Step 3: 实现 readDb/writeDb/find/append**

```typescript
// 追加到 mcp-server/src/db.ts

export async function readDb(projectRoot: string): Promise<AptDb> {
  const dbPath = getDbPath(projectRoot);
  try {
    const raw = await fs.readFile(dbPath, "utf-8");
    return JSON.parse(raw) as AptDb;
  } catch (err: unknown) {
    const code = (err as NodeJS.ErrnoException).code;
    if (code === "ENOENT") {
      throw new Error(
        "❌ .ai/db.json not found. Run agent-init first."
      );
    }
    throw new Error(`❌ Failed to read db.json: ${String(err)}`);
  }
}

export async function writeDb(projectRoot: string, db: AptDb): Promise<void> {
  const aiDir = getAiDir(projectRoot);
  await fs.mkdir(aiDir, { recursive: true });
  await fs.writeFile(getDbPath(projectRoot), JSON.stringify(db, null, 2), "utf-8");
}

export function findContract(db: AptDb, name: string): Contract | undefined {
  return db.contracts.find((c) => c.name === name);
}

export async function appendContract(
  projectRoot: string,
  contract: Contract
): Promise<void> {
  const db = await readDb(projectRoot);
  if (findContract(db, contract.name)) {
    throw new Error(
      `❌ Contract '${contract.name}' already exists. Registration aborted.`
    );
  }
  db.contracts.push(contract);
  await writeDb(projectRoot, db);
}

export async function appendMissing(
  projectRoot: string,
  req: MissingRequest
): Promise<void> {
  const db = await readDb(projectRoot);
  db.missingRequests.push(req);
  await writeDb(projectRoot, db);
}
```

- [ ] **Step 4: 运行 test**

Run: `cd mcp-server && npm test`  
Expected: db tests PASS

---

### Task 4: INDEX.md 生成器

**Files:**
- Create: `mcp-server/src/index-md.ts`
- Create: `mcp-server/tests/index-md.test.ts`

- [ ] **Step 1: 写 failing test**

```typescript
// mcp-server/tests/index-md.test.ts
import { describe, it, expect } from "vitest";
import { renderIndexMd } from "../src/index-md.js";
import type { AptDb } from "../src/db.js";

describe("index-md", () => {
  it("renders contracts and missing tables", () => {
    const db: AptDb = {
      contracts: [
        {
          name: "Foo",
          description: "desc",
          tsFilePath: "src/foo.ts",
          registeredAt: "2026-06-01T00:00:00.000Z",
        },
      ],
      missingRequests: [
        {
          missingName: "Bar",
          reason: "need it",
          reportedAt: "2026-06-01T01:00:00.000Z",
        },
      ],
    };
    const md = renderIndexMd(db);
    expect(md).toContain("# Agent Protocol Contract Index");
    expect(md).toContain("| Foo |");
    expect(md).toContain("| Bar |");
  });

  it("renders _None._ when empty", () => {
    const md = renderIndexMd({ contracts: [], missingRequests: [] });
    expect(md).toContain("_None._");
  });
});
```

- [ ] **Step 2: 实现 renderIndexMd + writeIndexMd**

```typescript
// mcp-server/src/index-md.ts
import fs from "node:fs/promises";
import type { AptDb } from "./db.js";
import { getIndexMdPath } from "./paths.js";

function escapeCell(s: string): string {
  return s.replace(/\|/g, "\\|").replace(/\n/g, " ");
}

export function renderIndexMd(db: AptDb): string {
  const contractRows =
    db.contracts.length === 0
      ? "| _None._ | | | |"
      : db.contracts
          .map(
            (c) =>
              `| ${escapeCell(c.name)} | ${escapeCell(c.description)} | ${escapeCell(c.tsFilePath)} | ${c.registeredAt} |`
          )
          .join("\n");

  const missingRows =
    db.missingRequests.length === 0
      ? "| _None._ | | |"
      : db.missingRequests
          .map(
            (m) =>
              `| ${escapeCell(m.missingName)} | ${escapeCell(m.reason)} | ${m.reportedAt} |`
          )
          .join("\n");

  return `# Agent Protocol Contract Index

> Auto-generated by agent-protocol-mcp. Do not edit manually.

## Registered Contracts

| Name | Description | TS File | Registered |
|------|-------------|---------|------------|
${contractRows}

## Pending Missing Requests

| Name | Reason | Reported |
|------|--------|----------|
${missingRows}
`;
}

export async function writeIndexMd(projectRoot: string, db: AptDb): Promise<void> {
  await fs.writeFile(getIndexMdPath(projectRoot), renderIndexMd(db), "utf-8");
}

export async function regenerateIndexMd(projectRoot: string): Promise<void> {
  const { readDb } = await import("./db.js");
  const db = await readDb(projectRoot);
  await writeIndexMd(projectRoot, db);
}
```

- [ ] **Step 3: 运行 test**

Run: `cd mcp-server && npm test`  
Expected: all tests PASS

---

### Task 5: MCP Server 入口（3 Tools）

**Files:**
- Create: `mcp-server/src/index.ts`

- [ ] **Step 1: 实现 MCP Server**

```typescript
// mcp-server/src/index.ts
#!/usr/bin/env node
import { McpServer } from "@modelcontextprotocol/sdk/server/mcp.js";
import { StdioServerTransport } from "@modelcontextprotocol/sdk/server/stdio.js";
import { z } from "zod";
import fs from "node:fs/promises";
import { readDb, appendContract, appendMissing, findContract } from "./db.js";
import { getProjectRoot, resolveTsPath } from "./paths.js";
import { regenerateIndexMd } from "./index-md.js";

const projectRoot = getProjectRoot();

const server = new McpServer({
  name: "agent-protocol-mcp",
  version: "1.0.0",
});

server.tool(
  "query_contract",
  "Query dependency contract and TS type definition before development",
  { name: z.string().describe("Contract or component name") },
  async ({ name }) => {
    try {
      const db = await readDb(projectRoot);
      const contract = findContract(db, name);
      if (!contract) {
        return {
          content: [
            {
              type: "text" as const,
              text: "❌ Contract not found. You must call report_missing to block this task.",
            },
          ],
          isError: true,
        };
      }
      const absPath = resolveTsPath(projectRoot, contract.tsFilePath);
      let tsContent: string | null = null;
      try {
        tsContent = await fs.readFile(absPath, "utf-8");
      } catch {
        tsContent = null;
      }
      const payload = {
        description: contract.description,
        tsFilePath: contract.tsFilePath,
        tsContent,
        ...(tsContent === null ? { warning: "TS file unreadable" } : {}),
      };
      return {
        content: [{ type: "text" as const, text: JSON.stringify(payload, null, 2) }],
      };
    } catch (err) {
      return {
        content: [{ type: "text" as const, text: String(err) }],
        isError: true,
      };
    }
  }
);

server.tool(
  "register_contract",
  "Register a new contract after feature completion",
  {
    name: z.string(),
    description: z.string(),
    tsFilePath: z.string(),
  },
  async ({ name, description, tsFilePath }) => {
    try {
      const absPath = resolveTsPath(projectRoot, tsFilePath);
      try {
        await fs.access(absPath);
      } catch {
        return {
          content: [
            {
              type: "text" as const,
              text: "❌ TS file not found. Registration aborted.",
            },
          ],
          isError: true,
        };
      }
      await appendContract(projectRoot, {
        name,
        description,
        tsFilePath,
        registeredAt: new Date().toISOString(),
      });
      await regenerateIndexMd(projectRoot);
      return {
        content: [
          {
            type: "text" as const,
            text: "✅ Contract registered and INDEX.md updated.",
          },
        ],
      };
    } catch (err) {
      return {
        content: [{ type: "text" as const, text: String(err) }],
        isError: true,
      };
    }
  }
);

server.tool(
  "report_missing",
  "Report missing dependency and block current task",
  {
    missingName: z.string(),
    reason: z.string(),
  },
  async ({ missingName, reason }) => {
    try {
      await appendMissing(projectRoot, {
        missingName,
        reason,
        reportedAt: new Date().toISOString(),
      });
      await regenerateIndexMd(projectRoot);
      return {
        content: [
          {
            type: "text" as const,
            text: "🚫 TASK BLOCKED. Missing dependency reported to Manager Agent. Stop current work immediately and wait for resolution.",
          },
        ],
      };
    } catch (err) {
      return {
        content: [{ type: "text" as const, text: String(err) }],
        isError: true,
      };
    }
  }
);

async function main() {
  const transport = new StdioServerTransport();
  await server.connect(transport);
}

main().catch((err) => {
  console.error(err);
  process.exit(1);
});
```

- [ ] **Step 2: 添加 zod 依赖（SDK peer）**

在 `mcp-server/package.json` 的 `dependencies` 添加 `"zod": "^3.24.0"`

- [ ] **Step 3: 构建**

Run: `cd mcp-server && npm install && npm run build && npm test`  
Expected: build PASS, all tests PASS

---

### Task 6: 命令模板

**Files:**
- Create: `templates/start-feature.md`
- Create: `templates/finish-feature.md`
- Modify: `ref-docs/end-feature.md` → 重命名或同步为 `ref-docs/finish-feature.md`

- [ ] **Step 1: 创建 start-feature.md**

从 `ref-docs/start-feature.md` 复制内容到 `templates/start-feature.md`（内容已与 spec 一致）。

- [ ] **Step 2: 创建 finish-feature.md**

从 `ref-docs/end-feature.md` 复制内容到 `templates/finish-feature.md`。

- [ ] **Step 3: 同步 ref-docs**

创建 `ref-docs/finish-feature.md`（与 templates 相同）；保留或删除 `end-feature.md` 并在 README 注明已弃用。

---

### Task 7: agent-init 脚本

**Files:**
- Create: `bin/agent-init.sh`
- Create: `bin/agent-init.ps1`
- Create: `bin/agent-init.cmd`

- [ ] **Step 1: agent-init.sh**

```bash
#!/usr/bin/env bash
set -euo pipefail

APT_HOME="${APT_HOME:-$HOME/.apt}"
TARGET="$(pwd)"

mkdir -p "$TARGET/.claude/commands"
cp "$APT_HOME/templates/"*.md "$TARGET/.claude/commands/"

mkdir -p "$TARGET/.ai"
DB="$TARGET/.ai/db.json"
if [ ! -f "$DB" ]; then
  echo '{"contracts":[],"missingRequests":[]}' > "$DB"
fi

echo "✅ Agent Protocol Toolkit initialized. Commands injected."
```

- [ ] **Step 2: agent-init.ps1**

```powershell
$ErrorActionPreference = "Stop"
$aptHome = if ($env:APT_HOME) { $env:APT_HOME } else { Join-Path $env:USERPROFILE ".apt" }
$target = Get-Location

New-Item -ItemType Directory -Force -Path (Join-Path $target ".claude\commands") | Out-Null
Copy-Item (Join-Path $aptHome "templates\*.md") (Join-Path $target ".claude\commands\") -Force

New-Item -ItemType Directory -Force -Path (Join-Path $target ".ai") | Out-Null
$db = Join-Path $target ".ai\db.json"
if (-not (Test-Path $db)) {
  '{"contracts":[],"missingRequests":[]}' | Set-Content -Path $db -Encoding UTF8
}

Write-Host "✅ Agent Protocol Toolkit initialized. Commands injected."
```

- [ ] **Step 3: agent-init.cmd**

```bat
@echo off
powershell -NoProfile -ExecutionPolicy Bypass -File "%~dp0agent-init.ps1" %*
```

- [ ] **Step 4: Unix 可执行权限**

Run (Git Bash / macOS): `chmod +x bin/agent-init.sh`

---

### Task 8: install.sh（Unix）

**Files:**
- Create: `scripts/install.sh`

- [ ] **Step 1: 实现 install.sh**

```bash
#!/usr/bin/env bash
set -euo pipefail

REPO_ROOT="$(cd "$(dirname "$0")/.." && pwd)"
APT_HOME="${APT_HOME:-$HOME/.apt}"
CLAUDE_SETTINGS="$HOME/.claude/settings.json"
MCP_ENTRY="$APT_HOME/mcp-server/dist/index.js"

command -v node >/dev/null || { echo "Node.js 18+ required"; exit 1; }

echo "Building MCP server..."
(cd "$REPO_ROOT/mcp-server" && npm ci && npm run build && npm test)

echo "Deploying to $APT_HOME..."
mkdir -p "$APT_HOME/mcp-server" "$APT_HOME/templates" "$APT_HOME/bin"
rsync -a --delete "$REPO_ROOT/mcp-server/dist/" "$APT_HOME/mcp-server/dist/"
cp "$REPO_ROOT/mcp-server/package.json" "$APT_HOME/mcp-server/"
(cd "$APT_HOME/mcp-server" && npm ci --omit=dev)
cp -r "$REPO_ROOT/templates/"* "$APT_HOME/templates/"
cp "$REPO_ROOT/bin/"* "$APT_HOME/bin/"
cp "$REPO_ROOT/README.md" "$APT_HOME/"
chmod +x "$APT_HOME/bin/agent-init.sh"

echo "Merging Claude MCP settings..."
mkdir -p "$HOME/.claude"
node -e "
const fs=require('fs');
const p=process.argv[1];
const entry=process.argv[2];
let s={};
try{s=JSON.parse(fs.readFileSync(p,'utf8'));}catch(e){if(e.code!=='ENOENT')throw e;}
s.mcpServers=s.mcpServers||{};
s.mcpServers['agent-protocol-mcp']={command:'node',args:[entry]};
fs.writeFileSync(p,JSON.stringify(s,null,2));
" "$CLAUDE_SETTINGS" "$MCP_ENTRY"

# PATH
LINE='export PATH=\"\$HOME/.apt/bin:\$PATH\"'
for rc in \"$HOME/.bashrc\" \"$HOME/.zshrc\"; do
  [ -f \"\$rc\" ] || continue
  grep -q '.apt/bin' \"\$rc\" || echo \"\$LINE\" >> \"\$rc\"
done

echo "✅ APT installed to $APT_HOME"
echo "   Restart shell, then run: agent-init"
```

- [ ] **Step 2: chmod +x**

Run: `chmod +x scripts/install.sh`

---

### Task 9: install.ps1（Windows）

**Files:**
- Create: `scripts/install.ps1`

- [ ] **Step 1: 实现 install.ps1**

逻辑与 install.sh 等价：

1. `$RepoRoot` = 脚本上级目录
2. `$AptHome` = `$env:USERPROFILE\.apt`
3. `npm ci && npm run build && npm test` in mcp-server
4. Copy-Item dist, package.json, templates, bin, README
5. `npm ci --omit=dev` in `$AptHome\mcp-server`
6. 合并 `$env:USERPROFILE\.claude\settings.json`（用 Node 或 PowerShell ConvertFrom/To-Json）
7. 用户 PATH 追加 `%USERPROFILE%\.apt\bin`（`[Environment]::SetEnvironmentVariable` User scope）
8. 输出成功摘要

- [ ] **Step 2: 本地验证**

Run (PowerShell): `.\scripts\install.ps1`  
Expected: 部署到 `%USERPROFILE%\.apt\`，settings.json 含 agent-protocol-mcp

---

### Task 10: README

**Files:**
- Create: `README.md`

- [ ] **Step 1: 编写 README**

包含：

- 项目简介
- 前置条件（Node 18+, Claude Code）
- 安装：`./scripts/install.sh` 或 `.\scripts\install.ps1`
- 项目激活：`agent-init`
- MCP 三 Tool 说明
- 联调步骤（见 Task 11）
- 目录结构

---

### Task 11: 端到端联调

**Files:** 无（手动验证）

- [ ] **Step 1: 安装 APT**

Run: `./scripts/install.sh` (macOS) 或 `.\scripts\install.ps1` (Windows)

- [ ] **Step 2: 创建测试项目**

```bash
mkdir /tmp/apt-demo && cd /tmp/apt-demo
agent-init
```

Expected: `.claude/commands/` 含 2 个 md；`.ai/db.json` 存在

- [ ] **Step 3: 验证 MCP（可选 Node 脚本或 Claude Code）**

重启 Claude Code，确认 settings 中 MCP 已连接。

- [ ] **Step 4: 手动注册测试契约**

在项目创建 `src/contracts/demo.ts`:

```typescript
export interface DemoContract {
  id: string;
}
```

通过 Claude 调用 `register_contract` 或写脚本 stdin 测试。

- [ ] **Step 5: 验证 query 与 report_missing**

- `query_contract` name=`Demo` → 应返回类型内容
- `query_contract` name=`NonExistent` → 错误提示
- `report_missing` → db.json 与 INDEX.md 更新

- [ ] **Step 6: 验证 slash 命令**

Claude Code 中运行 `/start-feature`、`/finish-feature`，确认 Prompt 加载正确。

---

## Plan Self-Review

**Spec coverage:**

| Spec 章节 | Task |
|-----------|------|
| MCP 三 Tool | Task 5 |
| db.json schema | Task 3 |
| INDEX.md | Task 4 |
| agent-init | Task 7 |
| install + MCP merge | Task 8, 9 |
| templates | Task 6 |
| 跨平台 | Task 7, 8, 9 |
| 测试 | Task 2–5, 11 |

**无 TBD/占位符。** 所有文件路径与核心代码已给出。

---

## Execution Handoff

Plan complete and saved to `docs/superpowers/plans/2026-06-01-apt.md`.

**两种执行方式：**

1. **Subagent-Driven（推荐）** — 每个 Task 派发独立 subagent，任务间 review，迭代快
2. **Inline Execution** — 本会话按 Task 顺序直接实现，checkpoint Review

**请选择哪种方式开始实现？**
