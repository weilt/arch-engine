你现在已经完成了核心代码，必须执行闭环注册！

1. **提取契约**：检查你是否新建了可供外部调用的接口、类或函数。
2. **生成 TS 类型**：确保你的新接口在 `src/contracts/` 或对应目录下有严格的 TypeScript 类型定义文件。
3. **强制注册**：对于每一个新产出，你**必须调用 MCP 工具 `register_contract`**！传入名称、描述和 TS 文件路径。
4. 注册成功后，输出结构化完成报告。
